{{ define "actions_card" }}
<div class="card">
    <h3>Quick Actions</h3>
    <div class="card-content">
        <div class="cyberpunk-actions">
            <button class="btn" id="view-agents-btn"><span class="btn-icon">⚡</span> View Agents</button>
            <button class="btn" id="generate-stager-btn"><span class="btn-icon">🚀</span> Generate Stager</button>
        </div>
    </div>
</div>
{{ end }}